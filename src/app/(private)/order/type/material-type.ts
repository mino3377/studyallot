export type MaterialRow = {
    id: string, title: string, project_id: string, order: number
}

export type adjustedMaterialRow = {
    id: string
    slug: string,
    projectId: string,
    title: string,
}